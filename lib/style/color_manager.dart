import 'package:flutter/material.dart';

class ColorManage {
  static List<Color> colorArray = [
    sandGlassStart,
    timeTableStart,
    profileStart,
  ];

  // sandGlass
  static const Color sandGlassStart = Color(0xFF6AD4DD);
  // timeTable
  static const Color timeTableStart = Color(0xFF7AA2E3);
  // 个人信息模块
  static const Color profileStart = Color(0xFFF8F6E3); // sandGlass

  static const Color sandGlassUn = Color(0xFFc5e5e7);
  // timeTable
  static const Color timeTableUn = Color(0xFFd4deee);
  // 个人信息模块
  static const Color profileUn = Color(0xFFffffff);
  static const Color unSelectedStart = Colors.white;
}
